<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Shadow Layer -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#20000000"/> <!-- Reduced shadow color opacity -->
            <corners android:radius="10dp"/> <!-- Rounded corners for shadow -->
            <padding
                android:left="5dp"
                android:top="5dp"
                android:right="5dp"
                android:bottom="5dp"/>
        </shape>
    </item>

    <!-- Main Shape Layer -->
    <item android:top="2dp" android:left="2dp"> <!-- Adjusted offset for a subtler shadow -->
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF"/> <!-- Background color -->
            <corners android:radius="10dp"/> <!-- Rounded corners -->
            <padding
                android:left="5dp"
                android:top="5dp"
                android:right="5dp"
                android:bottom="5dp"/> <!-- Padding inside the EditText -->
        </shape>
    </item>

</layer-list>
